const String tableProjectParticipants = 'projectParticipants';

class ProjectParticipantFields {
  static const values = [
    projectId,
    participantId,
    lastUpdate,
  ];

  static const String projectId = 'project_id';
  static const String participantId = 'participant_id';
  static const String lastUpdate = 'last_update';
}
